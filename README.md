# lab02tut

This project contains a simple C++ print function.

Project structure:

- `include/print.hpp` — header file with function declarations.
- `sources/print.cpp` — source file with function implementation.
- `examples/example1.cpp` — example that prints text to console.
- `examples/example2.cpp` — example that prints text to file.
